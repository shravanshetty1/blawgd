Make sure numeric values cannot overflow
validate if post id already exists before broadcast
use relational database
frontend component based architecture - all components have their own css and js file